{
	"cmd": [
		"echo Building && g++ ${file} -o ${file_path}/${file_base_name} && echo Build Finished && ${file_path}/${file_base_name}.exe"],
	"file_regex": "^(..[^:]*):([0-9]+):?([0-9]+)?:? (.*)$",
	"working_dir": "${file_path}",
	"selector": "source.c, source.cpp, source.c++",
	"path": "M:Versions/MinGW/bin",
	"shell": true,
	"variants": [
		{
			"name": "Run",
			"cmd": ["${file_path}/${file_base_name}.exe"]
		}
	]
}